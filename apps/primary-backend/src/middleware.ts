import { NextFunction , Request } from "express"
export const authmiddleware = async (req: Request, next: NextFunction , res : Response) => {
    // try {
    // } catch (error) {
    //     console.error('Middleware error:', error)
    //     throw error
    // }
}